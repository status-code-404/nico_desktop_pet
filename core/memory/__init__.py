"""
Nicole Memory System — user profile + vector RAG + episodic timeline + task engine.

Usage:
    from core.memory import collector, retriever, tasks, profile

    collector.record("user", "我今天吃了川菜")
    results = retriever.search("昨晚做了什么")
"""
